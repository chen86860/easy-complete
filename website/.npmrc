# This project intentionally tracks current toolchain releases (Vite/esbuild,
# Wrangler/workerd). Disable the global minimum-release-age gate here so those
# freshly published versions aren't quarantined with empty package dirs.
minimum-release-age=0
min-release-age=0
